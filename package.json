{
  "name": "fluiid-static",
  "type": "module",
  "version": "0.1.0",
  "description": "Site statique Astro généré depuis l'API WordPress de fluiid.ch — migration SEO-safe",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "fetch:wp": "node scripts/fetch-wp.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0"
  }
}
